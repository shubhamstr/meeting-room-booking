PORT=5000
NODE_ENV=development

# -------------------------------------------------------------
# PostgreSQL Database Configuration
# -------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=meeting-room-bookings

# -------------------------------------------------------------
# Zoho CRM Integration (APIs under /api/zoho)
# -------------------------------------------------------------
ZOHO_API_CLIENT_ID=
ZOHO_API_CLIENT_SECRET=
ZOHO_DATA_CENTER=USDataCenter
ZOHO_REDIRECT_URI=http://localhost:5000/api/zoho/callback
ZOHO_CURRENT_USER_EMAIL=shubhamsutar5799@gmail.com

# -------------------------------------------------------------
# Google Calendar Integration (APIs under /api/calendar)
# -------------------------------------------------------------
# Alternatively automatically loaded from client_secret_*.json
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/api/calendar/callback
# Target Google Calendar ID (e.g. 'primary' or shared calendar ID: 'c_xxxx@group.calendar.google.com')
GOOGLE_CALENDAR_ID=
GOOGLE_SHARED_CALENDAR_ID=
GOOGLE_CALENDAR_TIMEZONE=Asia/Kolkata